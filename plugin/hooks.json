{
  "version": 1,
  "hooks": {
    "preToolUse": [
      {
        "type": "command",
        "powershell": "if (Test-Path .github\\tools\\owner_validator.py) { python .github\\tools\\owner_validator.py --hook --org org.json } else { '{\"permissionDecision\":\"allow\"}' }",
        "bash": "if [ -f .github/tools/owner_validator.py ]; then python .github/tools/owner_validator.py --hook --org org.json; else echo '{\"permissionDecision\":\"allow\"}'; fi",
        "timeoutSec": 20
      }
    ]
  }
}
